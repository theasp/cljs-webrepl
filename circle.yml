machine:
  environment:
    _JAVA_OPTIONS: -Xmx1g
  java:
    version: oraclejdk8

general:
  branches:
    ignore:
      - gh-pages

test:
  pre:
    - lein with-profile frontend-prod cljsbuild once frontend
    - lein with-profile backend-prod cljsbuild once backend

deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh
