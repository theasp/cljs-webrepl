machine:
  environment:
    _JAVA_OPTIONS: -Xmx1g
  java:
    version: oraclejdk8

general:
  branches:
    ignore:
      - gh-pages

test:
  pre:
    - lein build-min

deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh
